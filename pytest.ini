[pytest]
testpaths = tests
addopts = -q --strict-markers

# Note: fastmcp imports authlib, which emits a deprecation warning while
# conftest imports mcp_legalize — before pytest installs its warning filters.
# It cannot be suppressed from here and is not actionable in this repo.
