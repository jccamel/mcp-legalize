# mcp-legalize environment configuration
# Path to the directory containing index_*.json files.
# Repo paths are resolved via each index's `_meta.directorio_base` field.
LEGALIZE_INDICES_DIR=./indices
LEGALIZE_DEFAULT_LIMIT=20
LEGALIZE_MAX_CONTENT_CHARS=80000
